const AuthValidation = () => {
  return <div></div>;
};

/**
 * We need a header1, header2, header3
 * We need a card
 * We need an input
 * We need a button
 * We need a Text
 * We need a Checkbox
 * We need an Icon
 * We need a Link
 * We need a Separator
 * We need a Form
 */

export default AuthValidation;
